#!/usr/bin/env bash

echo "-----> Running post-compile hook"
echo "-----> Installing Playwright and Chromium"

# Install Playwright
playwright install --with-deps --no-shell chromium

echo "-----> Finished installing Playwright and Chromium"

# Make sure the file is executable
chmod +x $0